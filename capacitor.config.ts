import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MerlinoApp2023',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
